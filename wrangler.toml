name = "reporter"
compatibility_date = "2024-12-12"

pages_build_output_dir = "dist"

[build]
command = "bun run build"

[env.production]
name = "reporter"
route = "reporter.greymass.io"

# Cloudflare Pages will automatically use Bun if bun.lock is present
# No additional environment variables needed
