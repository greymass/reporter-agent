---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import { extractHighlights } from '../utils/extractHighlights';
import githubStats from '../data/github.json';
import WhatsInsideSection from '../components/WhatsInsideSection.astro';
import ThreeWaysToRead from '../components/ThreeWaysToRead.astro';
import HowItWorksSection from '../components/HowItWorksSection.astro';
import FundedBySection from '../components/FundedBySection.astro';

// Get all reports and sort by date (newest first)
const allReports = await getCollection('docs');

// Filter for README files (summary reports)
const summaryReports = allReports
  .filter(report => report.id.toLowerCase().includes('readme'))
  .filter(report => !report.id.includes('test-')) // Exclude test reports
  .sort((a, b) => b.id.localeCompare(a.id));

// Extract year-month from report ID (e.g., "2025-11/README.md" -> "2025-11")
function getReportSlug(id: string) {
  return id.replace(/\/README\.md$/i, '').replace(/\/README$/i, '');
}

// Parse report title from ID
function getReportTitle(id: string) {
  const match = id.match(/(\d{4})-(\d{2})/);
  if (!match) return id;
  const [, year, month] = match;
  const monthNames = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
  return `${monthNames[parseInt(month) - 1]} ${year}`;
}

// Parse month/year for timeline display
function getMonthYear(id: string) {
  const match = id.match(/(\d{4})-(\d{2})/);
  if (!match) return { month: '', year: '' };
  const [, year, month] = match;
  const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
  return {
    month: monthNames[parseInt(month) - 1],
    year
  };
}

// Prepare timeline data with highlights
const timelineReports = summaryReports.map((report) => {
  // Use the report body directly from content collection
  const markdown = report.body || '';
  const highlights = extractHighlights(markdown);
  
  return {
    id: report.id,
    slug: getReportSlug(report.id),
    title: getReportTitle(report.id),
    monthYear: getMonthYear(report.id),
    highlights
  };
});

// Get the latest report slug for dynamic links
const latestReportSlug = timelineReports.length > 0 ? timelineReports[0].slug : '2025-11';
---

<BaseLayout title="Greymass Progress Reports" description="Monthly reports documenting open-source progress across Greymass and WharfKit blockchain infrastructure projects" latestReportSlug={latestReportSlug}>
  <!-- Hero Section -->
  <div class="relative overflow-hidden bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 -mt-16 pt-16">
    <div class="absolute inset-0 hero-dot-pattern opacity-50"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-base-100/10 to-transparent"></div>
    <div class="container mx-auto px-4 py-24 max-w-6xl relative">
      <div class="grid md:grid-cols-2 gap-12 items-start md:items-center">
        <!-- Left Column: Content -->
        <div class="text-left">
          <div class="inline-flex items-center gap-2 px-4 py-2 bg-purple-500/20 rounded-full text-purple-300 text-sm font-medium mb-6 border border-purple-500/30">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            Monthly Progress Reports
          </div>
          <h1 class="mb-6">
            <span class="block text-6xl md:text-7xl font-extrabold tracking-tight bg-gradient-to-r from-purple-300 via-purple-500 to-purple-700 bg-clip-text text-transparent leading-tight pb-2">Greymass</span>
            <span class="block text-3xl md:text-4xl font-semibold text-gray-300 mt-2">What We've Built</span>
          </h1>
          <p class="text-lg text-gray-300 mb-8 max-w-lg">
            A look inside Greymass projects with monthly progress reports. Browse executive summaries for stakeholders, technical documentation for developers, or raw research data. All reports are generated by an open-source multi-agent AI system.
          </p>
          <div class="flex gap-4 flex-wrap">
            <a href={`/reports/${latestReportSlug}/readme/`} class="btn btn-lg gap-2 bg-purple-600 hover:bg-purple-700 text-white border-0 shadow-none">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              Read Latest Report
            </a>
            <a href="#timeline" class="btn btn-outline btn-lg gap-2 text-white border-white/30 hover:bg-white/10 hover:border-white/50 shadow-none">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              Browse Timeline
            </a>
          </div>
        </div>
        
        <!-- Right Column: Stats Cards -->
        <div class="grid grid-cols-2 gap-4 md:pt-4">
          <!-- Monthly Reports -->
          <div class="bg-black/30 backdrop-blur-md rounded-2xl p-6 border border-primary/30 hover:border-primary/50 transition-colors">
            <div class="text-sm font-semibold text-primary uppercase tracking-wide mb-2">Reports</div>
            <div class="text-4xl font-bold text-white mb-1">{timelineReports.length}</div>
            <div class="text-sm text-gray-300">Monthly summaries</div>
          </div>
          
          <!-- Total Repositories -->
          <div class="bg-black/30 backdrop-blur-md rounded-2xl p-6 border border-secondary/30 hover:border-secondary/50 transition-colors">
            <div class="text-sm font-semibold text-secondary uppercase tracking-wide mb-2">Repositories</div>
            <div class="text-4xl font-bold text-white mb-1">{githubStats.totals.repositories}+</div>
            <div class="text-sm text-gray-300">{githubStats.totals.followers} followers</div>
          </div>
          
          <!-- Greymass Org -->
          <a href="https://github.com/greymass" target="_blank" rel="noopener" class="bg-black/30 backdrop-blur-md rounded-2xl p-6 border border-purple-500/30 hover:border-purple-500/50 transition-all group">
            <div class="text-xl font-bold text-white mb-1">Greymass</div>
            <div class="text-xs text-purple-300 font-mono mb-3 flex items-center gap-1">
              github.com/greymass
              <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 opacity-50 group-hover:opacity-100 transition-opacity" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
            </div>
            <div class="flex items-center gap-2 text-sm text-gray-300">
              <span>{githubStats.organizations.greymass.repositories}+ repos</span>
              <span>•</span>
              <span>{githubStats.organizations.greymass.followers} followers</span>
            </div>
          </a>
          
          <!-- WharfKit Org -->
          <a href="https://github.com/wharfkit" target="_blank" rel="noopener" class="bg-black/30 backdrop-blur-md rounded-2xl p-6 border border-cyan-500/30 hover:border-cyan-500/50 transition-all group">
            <div class="text-xl font-bold text-white mb-1">WharfKit</div>
            <div class="text-xs text-cyan-300 font-mono mb-3 flex items-center gap-1">
              github.com/wharfkit
              <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 opacity-50 group-hover:opacity-100 transition-opacity" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
            </div>
            <div class="flex items-center gap-2 text-sm text-gray-300">
              <span>{githubStats.organizations.wharfkit.repositories}+ repos</span>
              <span>•</span>
              <span>{githubStats.organizations.wharfkit.followers} followers</span>
            </div>
          </a>
          <a href="#about-reports" class="col-span-2 bg-gradient-to-br from-purple-500/20 to-cyan-500/20 rounded-2xl p-6 shadow-lg border border-purple-400/30 hover:border-purple-400/50 transition-all hover:shadow-xl group backdrop-blur-sm">
            <div class="flex items-center justify-between">
              <div>
                <div class="text-lg font-semibold text-white mb-1 flex items-center gap-2">
                  About These Reports
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-purple-300 group-hover:text-purple-200 transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                  </svg>
                </div>
                <div class="text-sm text-gray-300">What's inside and how they're made</div>
              </div>
              <div class="w-12 h-12 rounded-full bg-purple-500/30 flex items-center justify-center group-hover:bg-purple-500/40 transition-colors border border-purple-400/30">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-purple-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
            </div>
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Timeline Section -->
  <div id="timeline" class="container mx-auto px-4 py-16 max-w-5xl">
    <h2 class="text-3xl font-bold text-center mb-8">Development Timeline</h2>
    
    <div class="relative">
      <!-- Timeline line (hidden on mobile) -->
      <div class="hidden md:block absolute left-1/2 transform -translate-x-1/2 h-full w-0.5 bg-primary/20"></div>
      
      <div id="timeline-items">
      {timelineReports.slice(0, 6).map((report, index) => {
        const isLeft = index % 2 === 0;
        return (
          <div class="relative mb-8 md:mb-10">
            <div class={`flex flex-col md:grid md:grid-cols-2 md:gap-20 items-center`}>
              
              {/* Left side (even indices) */}
              {isLeft && (
                <>
                  {/* Card on left */}
                  <div class="w-full order-2 md:order-1 md:pr-8">
                    <div class="card bg-base-100 shadow-xl">
                      <div class="card-body">
                        <h3 class="card-title text-primary text-xl mb-3">{report.title}</h3>
                        
                        {report.highlights.length > 0 && (
                          <ul class="space-y-2 text-sm text-base-content/80 text-left">
                            {report.highlights.slice(0, 4).map(highlight => (
                              <li class="flex items-start gap-2">
                                <span class="text-primary flex-shrink-0 mt-0.5">▸</span>
                                <span>{highlight}</span>
                              </li>
                            ))}
                          </ul>
                        )}
                        
                        <div class="flex justify-between items-center mt-4 pt-4 border-t border-base-300">
                          <div class="flex gap-2">
                            <a href={`/reports/${report.slug}/readme/`} class="btn btn-sm btn-primary">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                              </svg>
                              Read Summary
                            </a>
                            <a href={`/reports/${report.slug}/technical/`} class="btn btn-sm bg-blue-600 hover:bg-blue-700 text-white border-0">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                              </svg>
                              Technical
                            </a>
                          </div>
                          <a href={`/reports/${report.slug}/research/`} class="btn btn-sm btn-ghost">
                            Research
                          </a>
                        </div>
                      </div>
                    </div>
                  </div>
                  
                  {/* Badge in center */}
                  <div class="flex justify-center order-1 md:order-2 mb-4 md:mb-0 md:absolute md:left-1/2 md:transform md:-translate-x-1/2 z-10">
                    <div class="flex flex-col items-center justify-center w-20 h-20 rounded-full bg-primary text-primary-content shadow-lg font-bold">
                      <div class="text-xl leading-none">{report.monthYear.month}</div>
                      <div class="text-sm opacity-90 mt-1">{report.monthYear.year}</div>
                    </div>
                  </div>
                </>
              )}
              
              {/* Right side (odd indices) */}
              {!isLeft && (
                <>
                  {/* Badge in center */}
                  <div class="flex justify-center order-1 mb-4 md:mb-0 md:absolute md:left-1/2 md:transform md:-translate-x-1/2 z-10">
                    <div class="flex flex-col items-center justify-center w-20 h-20 rounded-full bg-primary text-primary-content shadow-lg font-bold">
                      <div class="text-xl leading-none">{report.monthYear.month}</div>
                      <div class="text-sm opacity-90 mt-1">{report.monthYear.year}</div>
                    </div>
                  </div>
                  
                  {/* Card on right */}
                  <div class="w-full order-2 md:col-start-2 md:pl-8">
                    <div class="card bg-base-100 shadow-xl">
                      <div class="card-body">
                        <h3 class="card-title text-primary text-xl mb-3">{report.title}</h3>
                        
                        {report.highlights.length > 0 && (
                          <ul class="space-y-2 text-sm text-base-content/80 text-left">
                            {report.highlights.slice(0, 4).map(highlight => (
                              <li class="flex items-start gap-2">
                                <span class="text-primary flex-shrink-0 mt-0.5">▸</span>
                                <span>{highlight}</span>
                              </li>
                            ))}
                          </ul>
                        )}
                        
                        <div class="flex justify-between items-center mt-4 pt-4 border-t border-base-300">
                          <div class="flex gap-2">
                            <a href={`/reports/${report.slug}/readme/`} class="btn btn-sm btn-primary">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                              </svg>
                              Read Summary
                            </a>
                            <a href={`/reports/${report.slug}/technical/`} class="btn btn-sm bg-blue-600 hover:bg-blue-700 text-white border-0">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                              </svg>
                              Technical
                            </a>
                          </div>
                          <a href={`/reports/${report.slug}/research/`} class="btn btn-sm btn-ghost">
                            Research
                          </a>
                        </div>
                      </div>
                    </div>
                  </div>
                </>
              )}
              
            </div>
          </div>
        );
      })}
      </div>

      <!-- Hidden reports for "Show More" -->
      <div id="timeline-items-hidden" class="hidden">
      {timelineReports.slice(6).map((report, index) => {
        const actualIndex = index + 6; // Offset for proper left/right positioning
        const isLeft = actualIndex % 2 === 0;
        return (
          <div class="relative mb-8 md:mb-10">
            <div class={`flex flex-col md:grid md:grid-cols-2 md:gap-20 items-center`}>
              
              {/* Left side (even indices) */}
              {isLeft && (
                <>
                  {/* Card on left */}
                  <div class="w-full order-2 md:order-1 md:pr-8">
                    <div class="card bg-base-100 shadow-xl">
                      <div class="card-body">
                        <h3 class="card-title text-primary text-xl mb-3">{report.title}</h3>
                        
                        {report.highlights.length > 0 && (
                          <ul class="space-y-2 text-sm text-base-content/80 text-left">
                            {report.highlights.slice(0, 4).map(highlight => (
                              <li class="flex items-start gap-2">
                                <span class="text-primary flex-shrink-0 mt-0.5">▸</span>
                                <span>{highlight}</span>
                              </li>
                            ))}
                          </ul>
                        )}
                        
                        <div class="flex justify-between items-center mt-4 pt-4 border-t border-base-300">
                          <div class="flex gap-2">
                            <a href={`/reports/${report.slug}/readme/`} class="btn btn-sm btn-primary">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                              </svg>
                              Read Summary
                            </a>
                            <a href={`/reports/${report.slug}/technical/`} class="btn btn-sm bg-blue-600 hover:bg-blue-700 text-white border-0">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                              </svg>
                              Technical
                            </a>
                          </div>
                          <a href={`/reports/${report.slug}/research/`} class="btn btn-sm btn-ghost">
                            Research
                          </a>
                        </div>
                      </div>
                    </div>
                  </div>
                  
                  {/* Badge in center */}
                  <div class="flex justify-center order-1 md:order-2 mb-4 md:mb-0 md:absolute md:left-1/2 md:transform md:-translate-x-1/2 z-10">
                    <div class="flex flex-col items-center justify-center w-20 h-20 rounded-full bg-primary text-primary-content shadow-lg font-bold">
                      <div class="text-xl leading-none">{report.monthYear.month}</div>
                      <div class="text-sm opacity-90 mt-1">{report.monthYear.year}</div>
                    </div>
                  </div>
                </>
              )}
              
              {/* Right side (odd indices) */}
              {!isLeft && (
                <>
                  {/* Badge in center */}
                  <div class="flex justify-center order-1 mb-4 md:mb-0 md:absolute md:left-1/2 md:transform md:-translate-x-1/2 z-10">
                    <div class="flex flex-col items-center justify-center w-20 h-20 rounded-full bg-primary text-primary-content shadow-lg font-bold">
                      <div class="text-xl leading-none">{report.monthYear.month}</div>
                      <div class="text-sm opacity-90 mt-1">{report.monthYear.year}</div>
                    </div>
                  </div>
                  
                  {/* Card on right */}
                  <div class="w-full order-2 md:col-start-2 md:pl-8">
                    <div class="card bg-base-100 shadow-xl">
                      <div class="card-body">
                        <h3 class="card-title text-primary text-xl mb-3">{report.title}</h3>
                        
                        {report.highlights.length > 0 && (
                          <ul class="space-y-2 text-sm text-base-content/80 text-left">
                            {report.highlights.slice(0, 4).map(highlight => (
                              <li class="flex items-start gap-2">
                                <span class="text-primary flex-shrink-0 mt-0.5">▸</span>
                                <span>{highlight}</span>
                              </li>
                            ))}
                          </ul>
                        )}
                        
                        <div class="flex justify-between items-center mt-4 pt-4 border-t border-base-300">
                          <div class="flex gap-2">
                            <a href={`/reports/${report.slug}/readme/`} class="btn btn-sm btn-primary">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                              </svg>
                              Read Summary
                            </a>
                            <a href={`/reports/${report.slug}/technical/`} class="btn btn-sm bg-blue-600 hover:bg-blue-700 text-white border-0">
                              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                              </svg>
                              Technical
                            </a>
                          </div>
                          <a href={`/reports/${report.slug}/research/`} class="btn btn-sm btn-ghost">
                            Research
                          </a>
                        </div>
                      </div>
                    </div>
                  </div>
                </>
              )}
              
            </div>
          </div>
        );
      })}
      </div>

      <!-- Show More Button -->
      {timelineReports.length > 6 && (
        <div class="text-center mt-8">
          <button id="show-more-btn" class="btn btn-primary btn-lg">
            Show More Reports
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
        </div>
      )}
    </div>

    <script>
      document.getElementById('show-more-btn')?.addEventListener('click', function() {
        const hiddenItems = document.getElementById('timeline-items-hidden');
        const timelineItems = document.getElementById('timeline-items');
        const btn = this;
        
        if (hiddenItems && timelineItems) {
          // Move hidden items to visible container
          while (hiddenItems.firstChild) {
            timelineItems.appendChild(hiddenItems.firstChild);
          }
          // Hide the button
          btn.style.display = 'none';
        }
      });
    </script>
  </div>


  <WhatsInsideSection githubStats={githubStats} />

  <ThreeWaysToRead latestReportSlug={latestReportSlug} />

  <HowItWorksSection />

  <FundedBySection />
</BaseLayout>
